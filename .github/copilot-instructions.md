# Copilot Instructions

请先阅读仓库根目录的 `AGENTS.md` 和 `docs/AI-CODING-STANDARDS.md`。
本项目的 Java 控制面是唯一权威入口，Python 不管理平台任务状态，模型输出
不能替代 Policy Engine。遵循 Java 21、Spring AI 仅限 adapter、Conventional
Commits、测试优先和最小范围修改原则。
