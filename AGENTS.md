# AI 编程与工程协作规范

本文件是本仓库对 Codex、Copilot、Claude Code 及其他 AI 编程工具的统一约束。
人类贡献者也应遵守同一套边界。更具体的模块规则可以放在对应子目录的
`AGENTS.md` 中；离当前文件更近的规则优先，但不能违反本文件的安全约束。

## 1. 项目目标

本项目是企业 Agent 中台，不是单纯聊天机器人。首个业务闭环是：

`查询客户最近订单并创建售后任务 -> Supervisor -> Customer Agent -> Order Agent -> Policy Engine -> 人工审批 -> Support Tool -> 审计`

所有实现都必须服务于可治理、可审计、可恢复的任务执行。

## 2. 架构边界

- `java-platform` 是控制面和业务执行面的唯一权威入口，负责租户、身份、任务状态、审批、工具权限、审计和模型调用编排。
- `platform-domain` 只能使用 JDK；禁止依赖 Spring、Spring AI、数据库、HTTP 或 JSON 框架。
- `platform-application` 只定义用例和端口，不绑定模型供应商、Web 框架或基础设施。
- `adapters/spring-ai-2` 是唯一允许直接依赖 Spring AI 的模块；Spring AI 类型不得进入领域层、应用端口或跨语言契约。
- Python 只实现 RAG、文档解析、Embedding/Rerank、评测和 Computer Use 等专项能力；不得拥有平台任务状态，不得直接写业务库。
- 前端只调用 Java Platform API；不得直连模型、MCP、Python 内部接口或数据库。
- Agent 与 Tool 的跨进程交互必须通过已登记的 A2A/MCP 契约。
- 模块化单体是首期部署形态；不要为了“看起来微服务化”提前拆服务。

## 3. 安全与治理硬规则

- 任何模型输出都不是授权结果，必须经过 `PolicyEngine`。
- 高风险和关键写操作必须生成审批记录；不得通过配置开关默认绕过审批。
- 写工具必须具备权限检查、风险等级、幂等键、超时、重试边界和审计事件。
- 所有请求必须保留 `tenantId`、`userId`、`traceId`，跨 Agent 子任务必须继承租户和追踪上下文。
- 测试、示例和提交中不得出现真实 API Key、Cookie、密码、客户数据或生产连接串。
- 不得把敏感信息写入日志、Prompt、错误消息或 SSE 事件。
- 不得执行删除数据库、重置 Git、覆盖用户文件等破坏性操作，除非用户明确指定目标和范围。

## 4. AI 修改流程

1. 先读取目标模块的 README、邻近源码、契约和测试，再修改代码。
2. 先确认变更属于哪个模块；跨边界变更必须同时更新依赖说明和架构文档。
3. 小步提交：一个提交只完成一个可描述的目的。
4. 先写或更新测试，再实现行为；状态机、权限、审批、租户隔离和协议解析必须有负向测试。
5. 修改 OpenAPI、JSON Schema、A2A 或 MCP 契约时，必须更新示例、兼容说明和消费者测试。
6. 不要凭空升级 Spring Boot、Spring AI、JDK、Node、Python 或第三方 SDK；先更新版本矩阵并验证兼容性。
7. 如果环境无法运行测试，必须在交付说明中明确写出未验证项，不得声称“构建通过”。
8. 修改完成后检查 `git diff`、敏感信息、未跟踪文件和文档状态。

## 5. 语言规范

### Java

- 使用 Java 21 语言基线；优先使用不可变对象、`record` 和显式端口。
- 领域代码禁止使用 Lombok、反射式框架注解或持久化注解。
- 业务规则放在领域服务或应用用例，不放在 Controller、Mapper 或配置类。
- 异常必须表达可处理的业务原因；不得吞掉异常或用空 catch。
- 任何外部调用都必须有超时、取消、重试上限和可观测字段。

### Python

- 使用 Python 3.12 和 `uv`；类型标注公共函数和 Pydantic 边界模型。
- Python 服务只通过明确的 HTTP/消息契约与 Java 通信。
- 不在 Python 服务中复制任务状态机或实现第二套审批逻辑。

### TypeScript/Next.js

- 前端只依赖公开 Platform API 和 SSE。
- 服务端与浏览器端的边界必须清晰；不要把密钥发送到浏览器。
- 所有异步任务页面必须处理加载、空数据、失败、超时和权限不足状态。

## 6. 测试最低要求

- 领域状态机：覆盖合法转换、非法转换、终态不可变更。
- Policy Engine：覆盖允许、拒绝、需审批、缺少幂等键和越权角色。
- API/契约：覆盖鉴权、租户隔离、错误响应和向后兼容。
- 持久化：覆盖乐观锁、重复消息、Outbox 重投和幂等。
- Agent 编排：覆盖超时、重试、子任务失败、人工审批恢复和审计链。

## 7. Git 规范

- 默认分支为 `main`；功能分支使用 `feat/`、`fix/`、`docs/`、`refactor/`、`test/` 前缀。
- 提交遵循 Conventional Commits，例如 `feat(policy): add high-risk approval gate`。
- 提交正文说明行为变化、测试结果和配置/契约影响。
- 不提交 `.env`、构建产物、数据库文件、IDE 配置、密钥和本地缓存。
- Pull Request 必须说明变更范围、风险、测试命令和回滚方式。

## 8. 完成定义

一个变更只有在代码、测试、契约、文档和 Git 状态都一致时才算完成。
如果存在环境阻塞，交付结果必须列出阻塞原因和下一条可执行命令。
