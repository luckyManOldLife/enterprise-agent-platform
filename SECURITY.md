# Security Policy

## 支持范围

安全修复优先覆盖 `main` 分支和最新发布版本。开发中的实验模块可能尚未达到
生产安全等级，不能直接用于处理真实客户数据。

## 报告漏洞

请使用 GitHub Security Advisories 的私密报告入口提交漏洞，不要在公开 Issue
中粘贴密钥、个人信息、生产地址或可直接利用的完整 PoC。报告应包含：

- 受影响的提交、版本或模块。
- 复现步骤和影响范围。
- 是否涉及租户越权、审批绕过、数据泄露或远程执行。
- 建议的修复方向（如有）。

维护者确认后会在修复、回归测试和发布说明完成后公开处理结果。

## 安全开发约束

- 不提交真实凭据和生产配置。
- 模型输出必须经过 Policy Engine。
- 高风险写操作必须具备人工审批和审计。
- Python 服务不能直接写平台数据库或业务系统。
