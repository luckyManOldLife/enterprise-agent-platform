# platform-application

用例编排和端口定义。定义 `TaskUseCase`、`ApprovalUseCase`、`AgentRegistryPort`、`AgentInvokerPort`、`ToolRegistryPort`、`TaskRepositoryPort`、`ApprovalRepositoryPort`、`TaskEventStorePort`、`ModelGatewayPort`、`ToolExecutorPort` 和 `PolicyEnginePort`，不绑定具体实现。
